import {RFValue} from '@shared-import';
import {primary} from './fonts';

export const DEFAULT_ICON_SIZE = RFValue(20);

export default {
  primary: {
    fontFamily: primary.regular,
  },
  primaryItalic: {
    fontFamily: primary.italic,
  },
  primaryBold: {
    fontFamily: primary.bold,
  },
};
